# Live Poll — fixed CSP (No Native Modules)

Ten wariant ma:
- CSP bez inline-JS (Helmet)
- Chart.js z CDN (jsDelivr) z odpowiednim `script-src` i `connect-src`
- Front przeniesiony do `public/app.js`

## Start
```bash
npm install
npm run start   # http://localhost:5171
```

## Reset danych
```bash
npm run reset
```
